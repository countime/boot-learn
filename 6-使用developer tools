这个功能是方便学习和开发环境使用的,生产环境不会使用这个功能.

1. 唯一需要做的就是
  引入 spring-boot-devtools jar包. 注意他不是starter,所以里面是真正有class的jar包.

引入这个jar包后,spring 项目只要发现项目重新编译,就会自动重启,而且使用了
分离classloader的方式,能够快速重启. 非常方便调试.
如果对原理感兴趣,可以百度.

主要还是使用他来方便调试,做实验非常方便,修改下代码,eclipse重新build项目就行了. 只要看到控制台输出了
spring启动的日志,就说明类重新加载了.
